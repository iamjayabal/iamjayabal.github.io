import { MousehovereventDirective } from './mousehoverevent.directive';

describe('MousehovereventDirective', () => {
  it('should create an instance', () => {
    const directive = new MousehovereventDirective();
    expect(directive).toBeTruthy();
  });
});
