import { PascalcaseDirective } from './pascalcase.directive';

describe('PascalcaseDirective', () => {
  it('should create an instance', () => {
    const directive = new PascalcaseDirective();
    expect(directive).toBeTruthy();
  });
});
